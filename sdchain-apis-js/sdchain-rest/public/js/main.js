$(".close").on('click', function(event) {
	event.preventDefault();
	/* Act on the event */
	$(".submit_layer").hide(400)
});

$(".reset").on('click', function(event) {
	event.preventDefault();
	/* Act on the event */
	$(".submit_layer").hide(400)
});